"""
will complete later should set up the llm stuff based on config, should be in config folder, .toml???


Dspy expects base url, api token , model and adapter... ollama very simliarly... hopefully dspy handles delivery failures... does not handle asynch calls well most likely m

"""

def setup(llm):
    """
    Should handle the setup of the dspy system for you


    
    Args:
        llm (str, optional): _description_. Defaults to "".
    """



    return

